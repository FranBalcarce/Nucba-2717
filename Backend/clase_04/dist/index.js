"use strict";
const sumar = (a, b) => {
    return a + b;
};
// console.log(5, 3);
//
function obtenerPrimerElemento(array) {
    return array[0];
}
const obtenerPrimerElemento2 = (array) => {
    return array[0];
};
const numeros = [1, 2, 3, 4, 5, 6, 7, 8];
const nombres = ["Agus", "Elias", "Facu"];
// obtenerPrimerElemento(numeros);
// obtenerPrimerElemento(nombres);
obtenerPrimerElemento2(numeros);
obtenerPrimerElemento2(nombres);
const punto = {
    x: 10,
    y: 20,
    color: "rojo",
};
